<UserControl x:Class="laba11.Views.ContactEditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:laba11.Views">
    <Grid Background="White">
        <StackPanel Margin="20">
            <TextBlock Text="Editing a contact" FontSize="20" FontWeight="Bold" Margin="10 0"/>

            <TextBlock Text="Contact Name:" Margin="10 0"/>
            <TextBox Text="{Binding EditContactName, UpdateSourceTrigger=PropertyChanged}" Margin="10 5"/>

            <TextBlock Text="Contact Phone Number:" Margin="10 0 "/>
            <TextBox Text="{Binding EditContactPhone, UpdateSourceTrigger=PropertyChanged}" Margin="10 5"/>

            <StackPanel Orientation="Horizontal">
                <Button Content="Save" FontSize="15" Command="{Binding SaveCommand}" Margin="10 5"/>
                <Button Content="Cancel" FontSize="15" Command="{Binding CancelCommand}" Margin="10 5"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
